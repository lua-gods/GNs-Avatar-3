local GNUI = require("lib.GNUI.main")

local Pages = require"lib.pages"
local Button = require"lib.GNUI.element.button"

local page = Pages.newPage("mailman")
---@param box GNUI.Box
page.INIT:register(function (box)
   
end)